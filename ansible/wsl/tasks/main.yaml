
---
# install
- name: tmux > openSUSE
  include_tasks: suse.yaml
  when: ansible_distribution == 'openSUSE Tumbleweed'

# wsl.conf
- name: wsl > distro > wsl.conf
  become: yes
  become_user: root
  copy:
    src: wsl.conf
    dest: /etc/wsl.conf
