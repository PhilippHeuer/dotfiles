---
# install
- name: fzf > arch
  include_tasks: arch.yaml
  when: ansible_distribution == 'Archlinux'
- name: fzf > bin install
  include_tasks: bininstall.yaml
  when: ansible_distribution != 'Archlinux'
