# nushell
config/nushell/history.txt

# user config
config/ssh/servers
config/git/user
config/fuzzmux/fuzzmux.user.yaml

# sops
config/sops/age/keys.txt

# files with local secrets
nix/nixos/pullsecrets.nix

# work in progress files
wip
.wip

# apply to host util scripts
apply

# docs
docs
